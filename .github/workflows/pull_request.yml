on: pull_request
name: Run Tests
jobs:
  runTests:
    name: Run Tests
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: Run Tests
      uses: actions/npm@master
      with:
        entrypoint: npm
        args: cit 
